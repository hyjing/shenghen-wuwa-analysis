# 安全与隐私

## 报告问题

如果发现可能泄露玩家数据、临时唤取链接或执行非预期网络请求的问题，请通过 GitHub Security Advisory 私下报告，不要创建包含敏感数据的公开 Issue。

报告时请提供能够复现问题的最少信息，并删除或替换以下内容：

- 游戏 UID、服务器标识；
- `record_id`、`resources_id` 和完整唤取临时链接；
- 游戏日志、Cookie、Token、手机号和邮箱；
- 未脱敏的抽卡 JSON。

## 数据处理边界

- 网页在浏览器本地解析并保存导入记录；
- 提取器只读取鸣潮日志，只请求库洛官方抽卡记录接口；
- 项目不提供账号登录，不收集密码、验证码、Cookie 或库街区 Token；
- 仓库维护者不会要求用户公开上传包含真实账号信息的文件。
